import { style } from "@vanilla-extract/css";

export const colorThemeSwitch = style({
  display: "flex",
});

export const colorThemeSwitchIcon = style({
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
  width: "100%",
  height: "100%",
});
